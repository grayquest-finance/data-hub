apiVersion: v2
name: data-hub
description: A standalone Go service that acts as a GraphQL aggregation layer between consumers and existing backends.
type: application
version: 0.1.0
appVersion: "1.0.0"
kubeVersion: ">=1.24.0-0"